Official NotchPulse macOS installer (.dmg) and application bundle (.app.zip) automatically packaged from commit {{COMMIT_SHA}}.

### Có gì mới trong bản v2.9.3.5:
- 🔓 **Sửa triệt để lỗi tự động ấn Enter mở máy Face ID**: Chuyển sang tín hiệu phần cứng thuần (`.hidSystemState`), hỗ trợ phím Return (`0x24`) & Keypad Enter (`0x4C`) giúp `SecurityAgent` nộp mật khẩu mở máy ngay lập tức.
- 👁️ **Giữ Notch khi quét trượt & Hover quét lại**: Khi mở máy quét chưa nhận diện được, tai thỏ vẫn giữ nguyên trên màn hình, chỉ cần rê chuột (hover) vào là tự động bật camera quét lại.
- 🎨 **Sửa sạch viền vuông mờ trên Màn hình khoá**: Khôi phục uỷ quyền SkyLight, loại bỏ hoàn toàn viền bóng xám thừa, bo góc tròn trong suốt 100% cho cả Media và Face ID.

